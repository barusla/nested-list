.cdx-nested-list {
  margin: 0;
  padding: 0;
  outline: none;
  counter-reset: item;
  list-style: none;

  &__item {
    line-height: 1.6em;
    display: flex;
    margin: 2px 0;

    [contenteditable]{
      outline: none;
    }

    &-body {
      flex-grow: 2;
    }

    &-content,
    &-children {
      flex-basis: 100%;
    }

    &-content {
      word-break: break-word;
      white-space: pre-wrap;
    }

    &-children {
    counter-increment: nested;

    }

    &::before {
      margin-right: 5px;
      white-space: nowrap;
    }
  }

  &--unordered > &__item::before {
    content: "•";
  }

  &--ordered > &__item::before {
    counter-increment: item;
    content: counters(item, ".") ". ";
  }

  &--alphabet > &__item::before {
    counter-increment: item;
    content: counters(item, ".", lower-alpha) ". ";
  }

  &--numberalpha > &__item::before {
    counter-increment: item;
    content: counters(item, ".") ". ";
  }

  &--alpharoman > &__item::before {
    counter-increment: item;
    content: counters(item, ".", lower-alpha) ". ";
  }

  &__item-children.cdx-nested-list--numberalpha > &__item::before {
    content: counter(item, lower-alpha) ". ";
  }
  
  &__item-children.cdx-nested-list--alpharoman > &__item::before {
    content: counter(item, lower-roman) ". ";
  }

  &__settings {
    display: flex;

    .cdx-settings-button {
      width: 50%;
    }
  }
}
